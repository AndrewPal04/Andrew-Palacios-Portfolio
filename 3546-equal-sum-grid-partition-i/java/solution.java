        for (int j = 0; j<n-1;j++){
            running += colSums[j];
            if (running * 2 == total){
        }
            }
                return true;
            if (running * 2 == total){
            running += rowSums[i];
        for (int i = 0; i<m-1;i++){
        long running = 0;
        }
            colSums[j] = sum;
            }
                sum += grid[i][j];
        running = 0;
